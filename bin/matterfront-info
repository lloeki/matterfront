#! /usr/bin/env node
var program = require("..");


program
  .parse(process.argv);

var pkgs = program.args;

  if (!pkgs.length){
    console.error("packages required")
    process.exit(1);
  }

console.log("info about your installation");
